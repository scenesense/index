name: Set detail Blu-ray width

on:
  push:
    paths:
      - '.github/workflows/set-detail-bluray-150.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set detail Blu-ray logo width only
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = '.detailBlurayBadge{position:absolute;left:0;top:86px;display:block;width:138px;height:52px;object-fit:contain;object-position:left center;filter:drop-shadow(0 7px 14px rgba(0,0,0,.48));pointer-events:none}'
          new = '.detailBlurayBadge{position:absolute;left:0;top:86px;display:block;width:150px;height:52px;object-fit:contain;object-position:left center;filter:drop-shadow(0 7px 14px rgba(0,0,0,.48));pointer-events:none}'
          if s.count(old) != 1:
              raise RuntimeError(f'Expected logo CSS once, found {s.count(old)}')
          s = s.replace(old, new, 1)
          score_markup = '<div class="scoreLine"><div class="overallScore" id="overallScore">—</div><div><div class="scoreCaption">overall score</div><div class="progressText" id="progressText">0 / 75 rated</div></div><img id="detailBlurayBadge" class="detailBlurayBadge" src="assets/format-logos/BLURAY.webp" alt="UHD Blu-ray" hidden></div>'
          if s.count(score_markup) != 1:
              raise RuntimeError('Score markup changed unexpectedly')
          p.write_text(s, encoding='utf-8')
          PY
      - name: Commit
        shell: bash
        run: |
          git config user.name "SceneSense maintenance"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          git commit -m "Set detail Blu-ray logo to 150px wide"
          git push origin HEAD:main
